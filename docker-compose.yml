version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: vbd-db-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: vbd_user
      POSTGRES_PASSWORD: vbd_password
      POSTGRES_DB: vbd_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vbd_user -d vbd_db"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

